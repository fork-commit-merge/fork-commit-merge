events {}

http {
    server {
        listen 8080;
        server_name localhost;

        root /tasks/nginx/easy;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}
