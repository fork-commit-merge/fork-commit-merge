function addNumbers(a, b) {
  return a + b;
}

// Example usage:
const num1 = 1;
const num2 = 2;
const sum = addNumbers(num1, num2);
console.log(`The sum of ${num1} and ${num2} is ${sum}.`); // This will output 3
