// JavaScript - Easy

// TODO: Write a function that returns the sum of two numbers
function sum(a, b) {
    const result = a + b;
    console.log(`The sum of ${a} and ${b} is ${result}.`);
    return result;
}
// usage example
sum(1, 2);
